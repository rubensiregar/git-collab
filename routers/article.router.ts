import { Router } from "express";
// silakan import yg lain
//
//
const route = Router();

// di sini kumpulan router
route.post("/", );
route.get("/", );
route.get("/:id", );
route.patch("/:id", );
route.delete("/:id", );

export default route;
